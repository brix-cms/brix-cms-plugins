# Demo of Plugins for the Brix CMS

Demo application for showcase of some Brix CMS plugins. 

##How to run
Start the web application with the Spring Boot maven plugin (`mvn spring-boot:run`). Or run org.brixcms.plugin.demo.BrixApplication as Java Application in your IDE. The application is running at [http://localhost:8080](http://localhost:8080). 
